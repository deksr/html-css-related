in the root folder run the command:
`sass --watch .`
this compiles the css inside the interpreter folder.


To get the css to compile inside the compiled folder run the command:
`sass --watch sass-interpreter-folder:sass-compiled-folder`

run ctr+c stop


************************************************************
  /*comments: these shows up on the browser. */
  //comments : these comments don't show up on the browser. Only for internal use. 

